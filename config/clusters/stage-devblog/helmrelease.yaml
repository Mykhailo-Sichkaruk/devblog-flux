apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: stage-devblog
  namespace: default
spec:
  interval: 20s
  chart:
    spec:
      chart: ./        
      sourceRef:
        kind: HelmRepository
        name: devblog-chart-repo
      version: "*" 
  values:
    replicaCount: 1
  upgrade:
    crds: CRDsPolicy 

