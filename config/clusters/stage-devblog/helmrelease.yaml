apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stage-devblog
spec:
  interval: 20s
  releaseName: app
  chart:
    spec:
      chart: stage-devblog
      sourceRef:
        kind: HelmRepository
        name: devblog-chart-repo
        namespace: default
      version: "*" 
  values:
    replicaCount: 1
  upgrade:
    crds: CreateReplace
