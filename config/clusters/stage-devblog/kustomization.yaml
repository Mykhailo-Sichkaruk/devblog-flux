apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: stage-devblog
  namespace: flux-system
spec:
  interval: 1m
  path: "./config/clusters/stage-devblog"
  prune: true
  sourceRef:
    kind: GitRepository
    name: stage-devblog
  validation: client
